# Direct dependencies only, with lower-bound pins for portability.
# (The previous file was a full `pip freeze` pinned to exact versions that don't
#  exist on the public PyPI, so clean installs failed. pip resolves transitive
#  deps — e.g. websocket-client, etc. — automatically.)
#
# For a reproducible deploy, after installing on the target run:
#   pip freeze > requirements.lock

# --- Broker / auth ---
smartapi-python>=1.4        # Angel One SmartAPI (REST + SmartWebSocketV2)
pyotp>=2.8                  # automated TOTP login

# --- Core runtime ---
requests>=2.28
python-dotenv>=1.0
schedule>=1.2

# --- Data / analytics ---
pandas>=2.0
numpy>=1.24
scipy>=1.10                 # greeks_engine: scipy.stats.norm (Black-Scholes)
tabulate>=0.9              # trade journal table formatting

# --- LLM brain ---
# Install the provider set in LLM_PROVIDER (config/settings.py). Anthropic is the
# default; uncomment another if you switch providers.
anthropic>=0.40
# openai>=1.0
# groq>=0.4
